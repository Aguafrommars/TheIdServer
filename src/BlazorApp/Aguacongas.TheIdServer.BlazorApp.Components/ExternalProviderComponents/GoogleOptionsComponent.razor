@inherits ProviderOptionsBase<GoogleOptions>

<div class="form-group row">
    <label class="col col-form-label" for="clientId">
        client id
    </label>
    <div class="col-lg-10 col-sm-12">
        <AuthorizeText Id="clientId" Placeholder="client id" @bind-Value="@Model.Options.ClientId" />
    </div>
</div>
<div class="form-group row">
    <label class="col col-form-label" for="clientSecret">
        client secret
    </label>
    <div class="col-lg-10 col-sm-12">
        <AuthorizeText Id="clientSecret" Placeholder="client secret" @bind-Value="@Model.Options.ClientSecret" />
    </div>
</div>
<div class="form-group row">
    <label class="col col-form-label" for="accessType">
        access type
    </label>
    <div class="col-lg-10 col-sm-12">
        <AuthorizeText Id="accessType" Placeholder="access type" @bind-Value="@Model.Options.AccessType" />
    </div>
</div>
<OAuthOptionsProperties />
