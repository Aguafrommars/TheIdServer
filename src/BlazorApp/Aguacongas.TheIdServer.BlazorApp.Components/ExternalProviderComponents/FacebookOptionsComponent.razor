@inherits ProviderOptionsBase<FacebookOptions>

<div class="form-group row">
    <label class="col col-form-label" for="appId">
        app id
    </label>
    <div class="col-lg-10 col-sm-12">
        <AuthorizeText Id="appId" Placeholder="app id" @bind-Value="@Model.Options.AppId" />
    </div>
</div>
<div class="form-group row">
    <label class="col col-form-label" for="appSecret">
        app secret
    </label>
    <div class="col-lg-10 col-sm-12">
        <AuthorizeText Id="appSecret" Placeholder="app secret" @bind-Value="@Model.Options.AppSecret" />
    </div>
</div>
<div class="form-group row">
    <label class="col col-form-label" for="fields">
        fields
    </label>
    <div class="col-lg-10 col-sm-12">
        <ScopeCollection Id="fields" Placeholder="field" Collection="@Model.Options.Fields" />
    </div>
</div>
<div class="form-group row">
    <label class="col col-form-label" for="sendAppSecretProof">
    </label>
    <div class="col-lg-10 col-sm-12">
        <AuthorizeCheckbox Name="sendAppSecretProof" Label="send app secret proof" @bind-Value="@Model.Options.SendAppSecretProof" />
    </div>
</div>
<OAuthOptionsProperties />
