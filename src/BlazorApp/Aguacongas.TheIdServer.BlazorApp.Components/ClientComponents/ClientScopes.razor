@inject IStringLocalizerAsync<ClientScopes> Localizer
<div class="row">
    <div class="col">
        <ul class="list-inline">
            @foreach (var scope in Scopes)
            {
                <li @key="scope" class="list-inline-item">
                    <ClientScope Entity="@scope"
                                    Model="@Model"
                                    DeleteClicked="() => OnScopeDeleted(scope)" />
                </li>
            }
            <li class="list-inline-item">
                <ClientScope Entity="@_scope"
                                Model="@Model"
                                ValueChanged="() => OnScopeValueChanged(_scope)" />
            </li>
        </ul>
    </div>
</div>
<div class="row">
    <div class="col">
        <AuthorizeCheckbox Name="allow-offline-access" Label="@Localizer["allow offline access"]" @bind-Value="@Model.AllowOfflineAccess" />
    </div>
</div>
