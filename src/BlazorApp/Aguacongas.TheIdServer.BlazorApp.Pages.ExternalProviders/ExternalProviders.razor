@page "/externalproviders"
@inherits EntitiesModel<Entity.ExternalProvider>

<div class="row sticky-top bg-light">
    <div class="col">
        <PageListHeader Name="@Localizer["Providers"]" Url="externalprovider" ExportRequest="@ExportRequest" ExportDisabled="@ExportDisabled" />
    </div>
    <div class="col">
        <Filter TermChanged="OnFilterChanged" />
    </div>
    <EntitiesGrid Items="EntityList" TableClass="table mb-0">
        <TableHeader>
            <th><SelectAll GridState="GridState" /></th>
            <th class="w-25" scope="col">
                <SortableHeader Property="Id"
                                Text="@Localizer["id"]"
                                GridState="GridState" />
            </th>
            <th class="w-50" scope="col">
                <SortableHeader Property="DisplayName"
                                Text="@Localizer["name"]"
                                GridState="GridState" />
            </th>
            <th class="w-25" scope="col">
                <SortableHeader Property="SerializedHandlerType"
                                Text="@Localizer["type"]"
                                GridState="GridState" />
            </th>
        </TableHeader>
    </EntitiesGrid>
</div>
@if (EntityList == null)
{
    <Loading />
}
else
{
    <div class="row">
        <EntitiesGrid Items="EntityList" TableClass="table table-hover">
            <RowTemplate>
                <SelectableItem GridState="GridState" Item="@context" Selected="value => OnItemSelected(context.Id, value)" Context="item">
                    <td class="w-25" @onclick="() => OnRowClicked(item)">@item.Id</td>
                    <td class="w-50" @onclick="() => OnRowClicked(item)">@item.DisplayName</td>
                    <td class="w-25" @onclick="() => OnRowClicked(item)">@item.KindName</td>
                </SelectableItem>
            </RowTemplate>
        </EntitiesGrid>
    </div>
}

