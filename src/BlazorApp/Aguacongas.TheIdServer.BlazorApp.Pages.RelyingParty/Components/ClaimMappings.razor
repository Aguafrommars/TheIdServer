@inherits EntitesGridModel<Entity.RelyingPartyClaimMapping>

<EntitiesGrid Items="@Mappings" TableClass="table table-hover" Context="mapping">
    <TableHeader>
        <th scope="col">
            <SortableHeader Property="FromClaimType"
                            Text="@Localizer["from claim type"]"
                            GridState="GridState" />
        </th>
        <th scope="col">
            <SortableHeader Property="ToClaimType"
                            Text="@Localizer["to claim type"]"
                            GridState="GridState" />
        </th>
        <th></th>
    </TableHeader>
    <RowTemplate>
        <td>
            <AuthorizeText Id="fromClaimType" @bind-Value="@mapping.FromClaimType" />
        </td>
        <td>
            <AuthorizeText Id="toClaimType" @bind-Value="@mapping.ToClaimType" />
        </td>
        <td>
            <AuthorizeButton Type="button" CssSubClass="btn-primary" Clicked="() => OnDeleteEntityClicked(mapping)">
                <span class="oi oi-trash"></span>
            </AuthorizeButton>
        </td>
    </RowTemplate>
</EntitiesGrid>
